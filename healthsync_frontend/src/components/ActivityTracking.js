import React from "react";

// PUBLIC_INTERFACE
/**
 * ActivityTracking Stub Component
 * Placeholder for Activity Tracking feature (e.g. steps, workouts, exercise routines).
 */
function ActivityTracking() {
  return (
    <div style={{ padding: 24 }}>
      <h2>Activity Tracking</h2>
      <p>This section will allow users to log and monitor daily physical activities.</p>
      <span role="img" aria-label="activity" style={{ fontSize: 40 }}>🏃‍♂️</span>
      <div style={{ marginTop: 16, color: "#bbb", fontStyle: "italic" }}>
        (Feature module under construction)
      </div>
    </div>
  );
}

export default ActivityTracking;
