import React from "react";

// PUBLIC_INTERFACE
/**
 * HabitReminders Stub Component
 * Placeholder for Habit Reminders feature (e.g. reminders for workouts, hydration, etc).
 */
function HabitReminders() {
  return (
    <div style={{ padding: 24 }}>
      <h2>Habit Reminders</h2>
      <p>Set up custom reminders to maintain healthy habits.</p>
      <span role="img" aria-label="habit reminders" style={{ fontSize: 40 }}>⏰</span>
      <div style={{ marginTop: 16, color: "#bbb", fontStyle: "italic" }}>
        (Feature module under construction)
      </div>
    </div>
  );
}

export default HabitReminders;
